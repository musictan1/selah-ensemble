# 셀라앙상블 찬양단 서버 업데이트 매뉴얼

## 📋 개요
이 매뉴얼은 로컬에서 개발된 웹사이트를 실제 서버에 배포하는 방법을 설명합니다.

## 🎯 업데이트 대상 파일들

### 핵심 파일들
- `app.py` - Flask 서버 애플리케이션
- `music.html` - 음악 페이지 (업로드 기능 포함)
- `admin.html` - 관리자 페이지
- `data/` 폴더 - 사용자 및 데이터 파일들
- `uploads/` 폴더 - 업로드된 파일들

### 설정 파일들
- `data/users.json` - 사용자 계정 정보
- `data/data.json` - 음악/악보/비디오 데이터
- `data/menu_config.json` - 메뉴 권한 설정

## 🚀 배포 방법

### 방법 1: FTP/SFTP를 통한 배포 (가장 일반적)

#### 1.1 FTP 클라이언트 준비
- **FileZilla** (무료, 추천)
- **WinSCP** (Windows용)
- **Cyberduck** (Mac용)

#### 1.2 서버 정보 확인
호스팅 제공업체에서 제공하는 정보:
```
호스트: your-domain.com 또는 IP 주소
사용자명: FTP 계정명
비밀번호: FTP 비밀번호
포트: 21 (FTP) 또는 22 (SFTP)
```

#### 1.3 파일 업로드 순서
1. **서버 연결**
   - FTP 클라이언트에서 서버 정보 입력
   - 연결 후 웹사이트 루트 디렉토리로 이동

2. **핵심 파일 업로드**
   ```
   app.py → 서버 루트 디렉토리
   music.html → 서버 루트 디렉토리
   admin.html → 서버 루트 디렉토리
   ```

3. **데이터 폴더 업로드**
   ```
   data/ 폴더 전체 → 서버 루트 디렉토리
   ```

4. **업로드 폴더 업로드** (필요시)
   ```
   uploads/ 폴더 전체 → 서버 루트 디렉토리
   ```

### 방법 2: 웹 호스팅 제어판 사용

#### 2.1 파일 관리자 접속
- 호스팅 제공업체 제어판 로그인
- "파일 관리자" 또는 "File Manager" 클릭

#### 2.2 파일 업로드
1. 웹사이트 루트 디렉토리로 이동
2. "업로드" 버튼 클릭
3. 수정된 파일들 선택하여 업로드

### 방법 3: Git을 통한 배포

#### 3.1 GitHub 저장소 사용
```bash
# 변경사항 커밋
git add .
git commit -m "음악 업로드 기능 수정"
git push origin main
```

#### 3.2 자동 배포 설정
- GitHub Actions 또는 호스팅 제공업체의 Git 연동 기능 사용
- 저장소에 푸시하면 자동으로 서버에 배포됨

## 🔧 서버 재시작

### 로컬 테스트용 서버 재시작
```bash
# Windows PowerShell
taskkill /f /im python.exe
python app.py
```

### 실제 서버 재시작
호스팅 제공업체에 따라 다름:

#### 3.1 공유 호스팅
- 제어판에서 "서비스 재시작" 또는 "Restart Services" 클릭
- 또는 호스팅 제공업체에 문의

#### 3.2 VPS/전용 서버
```bash
# SSH로 서버 접속
ssh username@your-server.com

# Python 프로세스 종료
sudo pkill -f python
sudo pkill -f app.py

# 서버 재시작
cd /path/to/your/website
python app.py &
```

#### 3.3 클라우드 서비스 (AWS, GCP, Azure)
- 서비스 대시보드에서 인스턴스 재시작
- 또는 SSH를 통한 재시작

## 📁 파일별 업데이트 체크리스트

### 필수 업데이트 파일
- [ ] `app.py` - 서버 로직 변경시
- [ ] `music.html` - 음악 페이지 UI/기능 변경시
- [ ] `admin.html` - 관리자 페이지 변경시
- [ ] `data/users.json` - 사용자 정보 변경시
- [ ] `data/data.json` - 음악/악보 데이터 변경시

### 선택적 업데이트 파일
- [ ] `uploads/` 폴더 - 새 파일 추가시
- [ ] CSS/JS 파일들 - 디자인 변경시
- [ ] 기타 HTML 파일들 - 페이지 추가/수정시

## ⚠️ 주의사항

### 1. 백업 필수
업데이트 전 반드시 서버 파일 백업:
```bash
# 서버에서 백업 생성
cp -r /path/to/website /path/to/backup/$(date +%Y%m%d_%H%M%S)
```

### 2. 데이터 손실 방지
- `data/` 폴더 업데이트시 기존 데이터 확인
- 사용자 계정 정보 보존
- 업로드된 파일들 보존

### 3. 권한 설정
서버에서 파일 권한 확인:
```bash
# 디렉토리 권한
chmod 755 uploads/
chmod 755 data/

# 파일 권한
chmod 644 *.html
chmod 644 *.py
```

## 🔍 업데이트 후 확인사항

### 1. 기본 기능 테스트
- [ ] 웹사이트 접속 확인
- [ ] 로그인 기능 확인
- [ ] 음악 업로드 기능 확인 (관리자 계정)
- [ ] 파일 다운로드 기능 확인

### 2. 권한 테스트
- [ ] 관리자 계정으로 업로드 가능
- [ ] 일반 사용자는 업로드 불가
- [ ] 메뉴 접근 권한 확인

### 3. 오류 로그 확인
서버 오류 로그 확인:
```bash
# 로그 파일 확인
tail -f /path/to/error.log
```

## 🆘 문제 해결

### 업로드 후 웹사이트 접속 안됨
1. 서버 재시작 확인
2. 파일 권한 확인
3. 호스팅 제공업체에 문의

### 업로드 기능이 작동하지 않음
1. `music.html` 파일이 제대로 업로드되었는지 확인
2. 브라우저 캐시 삭제
3. 관리자 계정으로 로그인 확인

### 데이터가 사라짐
1. 백업에서 `data/` 폴더 복원
2. 호스팅 제공업체에 문의

## 📞 지원 연락처

### 호스팅 관련
- 호스팅 제공업체 고객센터
- 서버 관리자

### 개발 관련
- 개발자 연락처
- 기술 문서 참조

---

**마지막 업데이트**: 2024년 7월 30일
**버전**: 1.0 